# DocQuery N5.3-A7 新会话交接

> 交接日期：2026-08-24
> 当前状态：A7 定向复验失败，实验代码已回滚，等待讨论下一步最小方案

## 1. 必读顺序

新会话必须先完整阅读：

1. `docs/product/00-DocQuery-新服务交接说明.md`；
2. 交接说明第 2 节规定的全部产品文档和独立技术选型；
3. `docs/development/N5.3-A6-Answer定向复验.md`；
4. `docs/development/N5.3-A7-Grok-Agent提示词与Responses状态续接.md`；
5. 本文档。

不得根据旧对话印象或旧工程代码猜测当前能力。

## 2. 已确认完成的主阶段

- N0—N4.4 已实现、验证并经用户验收关闭；
- N5.1-P0/P1/P2 已完成，测试科技租户评测知识库 100/100 文档 READY；
- N5.2-R1 已经用户确认完成；
- N5.2-R3 已执行 80/80 个真实 HYBRID `/retrieve` 请求并形成报告，当前待用户验收；
- 多轮 Conversation、会话记忆和流式 Answer 仍未设计或实现。

N5.2-R3 结果：Doc Hit@1=`0.8000`、Doc Hit@5=`0.9250`、Doc Hit@10=`0.9625`、
Page Coverage@10=`0.6554`、MRR@10=`0.8585`，80/80 成功、0 降级、0 重试。

## 3. 当前供应商与运行基线

- Chat 已从 DeepSeek 全部替换为 OpenAI Responses 兼容的 PackyAPI；
- 实际模型为 `grok-4.6`，不是用户最初口述的 `grok-5.6`；
- Base URL 和 Key 只从现有配置/密钥文件读取，交接文档不得记录 Key；
- 百炼 Embedding 保持不变；
- 当前 Answer 运行基线已经回滚为 `answer-agent-v4 / answer-policy-v4`；
- 后端已用回滚代码重启，`http://localhost:8080/admin/` 健康检查为 200。

## 4. A6 与 A7 的真实结论

### A6

固定 5 题：HTTP 成功 5/5，人工正确 1/5，Gold 页覆盖率 0.20。主要问题是 Agent
过早拒答、同名章节消歧不足和数字任务完成度不足。

### A7

A7 只实验了更积极的提示词、工具说明和 Responses 无状态续接，没有改检索架构和文档数据。

协议验证确认 PackyAPI `grok-4.6` 支持：

- Function Calling；
- `store=false`；
- 加密 reasoning 续接；
- Prompt Cache。

第一次 A7 实现只续接“上一轮 output + 工具结果”，遗漏原始 system/user 上下文，属于实现错误，
只保留为诊断证据。修正为完整追加式上下文后执行 A7-v2，结果仍为：

- 逻辑请求成功 4/5；
- `ANSWERED` 1/5，受控拒答 3/5，最终供应商失败 1/5；
- 人工正确 0/5，Gold 页覆盖率 0.20；
- `0831`、`0937` 一次搜索后过早拒答，其中 `0937` 相比 A6 回退；
- `1032` 找到 Gold 第 24 页及 `133.2`、`120.1` 两个正确值，但没有完成求和 `253.3`。

因此 A7 为 `COMPLETED_FAILED_REVALIDATION / PENDING_USER_ACCEPTANCE`。提示词与正确状态续接
本身不足以解决问题，实验代码已回滚，不得把 A7 描述为已上线优化。

## 5. 验证证据

- A7 前完整免费回归：Java 180 项中 178 项通过、2 项真实供应商冒烟按设计跳过；
- 前端 Vitest 14/14；
- PackyAPI 最小协议冒烟及 Java 两轮真实 Tool Calling 冒烟通过；
- 回滚后的 Answer 定向单元测试 15/15；
- 回滚后后台健康检查为 HTTP 200。

主要证据：

- `evaluation/mmlongbench-docquery-v1/reports/n5.3-a7-v2-answer-agent-state/report.json`；
- 同目录 `raw-results.jsonl`、`manual-adjudication.json`、`audit-summary.json`；
- 首轮错误实现诊断：`evaluation/mmlongbench-docquery-v1/reports/n5.3-a7-answer-agent-state/`；
- A7 说明：`docs/development/N5.3-A7-Grok-Agent提示词与Responses状态续接.md`。

## 6. 下一步边界

下一步只讨论并冻结一个最小的单轮 Answer“任务完成检查”方案，目标是避免：

- 已拿到正确证据却漏算、漏答；
- 只搜索一次就过早返回 `INSUFFICIENT_EVIDENCE`；
- 通过不断增加提示词、工具或轮次形成无边界 Agent。

讨论时至少明确：完成条件如何机械判断、拒答条件、是否增加内部校验步骤、最大安全边界、
失败处理、只复验哪 5 题以及验收阈值。

没有用户新的明确开始授权前：

- 不修改代码；
- 不执行付费模型调用；
- 不扩大到 20/80 题 Answer；
- 不重新入库或重建 100 份文档；
- 不开始多轮 Conversation、会话记忆或流式 Answer。

## 7. 新会话启动提示词

```text
这是 DocQuery 项目的新会话，工作区是 D:\IdeaProjects\DocQuery。

请先完整阅读 docs/product/00-DocQuery-新服务交接说明.md，并严格按照其中第 2 节顺序完整阅读产品文档和独立技术选型；随后完整阅读 docs/development/N5.3-A6-Answer定向复验.md、docs/development/N5.3-A7-Grok-Agent提示词与Responses状态续接.md 和 docs/development/N5.3-A7-新会话交接.md。不要根据旧对话印象或旧工程代码直接推断。

当前 N0—N4.4、N5.1-P0/P1/P2 与 N5.2-R1 已完成并经用户验收关闭；N5.2-R3 已完成 80/80 个真实 HYBRID Retrieve 请求并待验收。多轮 Conversation、会话记忆和流式 Answer 尚未设计或实现。

当前 Chat 供应商为 PackyAPI OpenAI Responses 兼容接口，实际模型是 grok-4.6，百炼 Embedding 不变。N5.3-A7 已验证 Function Calling、加密 reasoning 无状态续接和 Prompt Cache 的协议能力，但修正状态续接后的固定 5 题人工正确仍为 0/5，因此 A7 失败，实验代码已回滚；当前运行基线是 answer-agent-v4 / answer-policy-v4，后端健康检查为 200。

下一步只讨论并冻结一个最小的单轮 Answer“任务完成检查”方案，解决拿到正确证据后漏算、漏答，以及搜索一次就过早拒答的问题。不要继续无边界增加提示词、工具或轮次。请先简要复述当前真实能力、A7 失败原因、当前运行基线和下一步边界，再与我讨论候选方案、机械完成条件、拒答条件、失败处理、固定 5 题复验方式和验收点。

在我给出新的明确开始语以前，不要修改代码、执行付费模型调用、扩大到 20/80 题 Answer、重建文档或开始多轮 Agent。仅“确认”“可以”不代表开始授权。
```

